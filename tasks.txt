Implement Phase 4 item 3 "Typed placeholder spec across the compile-time/runtime seam" from ROADMAP.md: replace handle_placeholder(..., caps: [&str; 5]) and the "~TENSE~MARKER:WORD" string sentinel with a typed struct/enum (e.g. PlaceholderSpec { article, pre_verb, case, tense, ... }) baked by the macro as a const. Delete the runtime re-parsing (strip_prefix("~TENSE~"), split_once(':'), fallback branches for states the macro can never produce) and the runtime re-recognition of articles by string-matching "the"/"a"/"some" in get_article_or_so. Document or disambiguate the overloaded '=' marker (subject case before the noun vs. continuous tense after it) while this lands. Run the full test suite. Mark the item's checkbox complete in ROADMAP.md when done.
Implement Phase 4 item 4 "Type the subject; remove runtime panics" from ROADMAP.md: make SubjectPronoun public with a FromStr impl and store it in Noun, so invalid subjects are unrepresentable instead of Noun::new panicking via assert!. Replace is_subjective_plural's discriminant comparison (as usize >= 6) with an explicit match. Make remaining fallible paths (inflect() on non-singularizable plurals, expect in is_subjective_plural) degrade gracefully instead of panicking. Run the full test suite. Mark the item's checkbox complete in ROADMAP.md when done.
Implement Phase 4 item 5 "Public API cleanup" from ROADMAP.md: rename inflect_possesive to inflect_possessive (public API, fix the typo). Add #[doc(hidden)] to macro plumbing (handle_placeholder, handle_placeholder_with_context, handle_tense_marker), matching HeedMatcher's existing treatment. Rework ack!()/nay!() to expand to expression forms (Ok(say!(...))/Err(say!(...))) instead of a hidden return, updating callers to write return themselves. Run the full test suite and update docs/examples referencing the old names. Mark the item's checkbox complete in ROADMAP.md when done.
Implement Phase 4 item 6 "Hand-written placeholder tokenizer" from ROADMAP.md: replace the PH_EXT regex internals with a small hand-written tokenizer that recognizes placeholder internals (keep the outer sigil grammar unchanged - it's the crate's identity). Produce precise error spans (e.g. "expected article or verb, found ...") instead of the blanket "Error in placeholder" message. Run the full test suite, including error-message assertions. Mark the item's checkbox complete in ROADMAP.md when done.
