Fix defect docs/architecture-review-2026-08-15.md section 1.9 (ROADMAP.md Phase 8 item 6): a negative count passed to a spelled-out numeral placeholder renders as the non-word "negativeone", because english_numbers::convert_no_fmt spells -1 that way; see the existing comment at src/lib.rs:509. Decide and implement the guard in ranting's own code rather than upstream: for a negative count, render "minus " followed by the spelled-out absolute value (so -1 becomes "minus one", -21 becomes "minus twenty-one"), keeping the existing leading-space handling in NumeralSpec::leading_space intact and leaving inflect_numeral_custom's contract unchanged (the hook must still see a single numeral string it can replace wholesale). Read .claude/rules/extension-hooks.md's inflect_numeral_custom paragraph first. Add integration tests in tests/ranting/numeral.rs covering -1, -21 and 0, update the src/lib.rs comment that documents the old behavior, add a CHANGELOG.md entry under Fixed, and mark section 1.9 fixed in docs/architecture-review-2026-08-15.md and in ROADMAP.md Phase 8 item 6. This is not a breaking change. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml (., ranting_core, ranting_derive, ranting_i18n, ranting_es, ranting_ar, ranting_ja, ranting_gaps, ranting_es_gaps) as CLAUDE.md requires.
Fix defect docs/architecture-review-2026-08-15.md section 1.7 (ROADMAP.md Phase 8 item 6): a plural proper name takes the possessive "'s" instead of the bare apostrophe, so {the Joneses'} renders "the Joneses's". The cause is is_name at src/lib.rs:1503, which decides "this is a name" from the first character alone and so exempts plural names from adapt_possesive_s's bare-apostrophe branch at src/lib.rs:1496. Fix it so a capitalized noun that is already plural (or is being rendered as plural) takes the bare apostrophe, while a singular name ending in s keeps "'s" -- the "Myles's" case pinned by the doctest just above adapt_possesive_s must keep rendering exactly as it does today, and so must every existing test. This IS a breaking change to rendered English output: say so explicitly in a CHANGELOG.md entry under a Breaking heading, in the wording CLAUDE.md's byte-identity invariant asks for. Add integration tests in tests/ranting/ (register any new module in tests/ranting/main.rs) covering a plural proper name, a singular name ending in s, and a plural common noun. Mark section 1.7 fixed in docs/architecture-review-2026-08-15.md and in ROADMAP.md Phase 8 item 6. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml (., ranting_core, ranting_derive, ranting_i18n, ranting_es, ranting_ar, ranting_ja, ranting_gaps, ranting_es_gaps) as CLAUDE.md requires.
Fix defect docs/architecture-review-2026-08-15.md section 1.6 (ROADMAP.md Phase 8 item 6): a phrasal or compound verb takes the bare third-person-singular -s on its last word instead of its head, so say!("{=0 pick up} the sword.") renders "He pick ups the sword.". The bug is in inflect_verb at src/language/english.rs:97, in the "he" | "she" | "it" arm: all three append branches (the sibilant + "es", the consonant-y + "ies", and the default + "s") inflect the whole trimmed string, and which one fires is decided by the spelling of the particle rather than of the verb, so {=0 stick to} gives "stick toes" and {=0 get by} gives "get bies". Fix all three branches by splitting the verb at its first whitespace, conjugating the head word, and re-appending the remainder unchanged; single-word verbs must be byte-identical to today, and the n't-stripping, the IrregularPluralVerb table lookup and the detect_tense early return must keep applying to the head word rather than to the whole string. Do not touch the tense-marked paths, which are already correct because ranting_core::verb_conjugate conjugates the head. This IS a breaking change to rendered English output: say so explicitly in a CHANGELOG.md entry under a Breaking heading. Add integration tests in tests/ranting/verb_tense.rs (or a new module registered in tests/ranting/main.rs) covering "pick up", "stick to", "get by" and a single-word control, in first, second and third person. Mark section 1.6 fixed in docs/architecture-review-2026-08-15.md and in ROADMAP.md Phase 8 item 6. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml (., ranting_core, ranting_derive, ranting_i18n, ranting_es, ranting_ar, ranting_ja, ranting_gaps, ranting_es_gaps) as CLAUDE.md requires.
Fix defect docs/architecture-review-2026-08-15.md section 1.10 (ROADMAP.md Phase 8 item 6): a space-separated head-first compound noun pluralizes on its tail, so {+attorney general} renders "attorney generals" and {+court martial} renders "court martials", while the hyphenated spellings attorney-general and court-martial are already correct. Read .claude/rules/pluralization.md in full first, especially point 6 ("adding a rule to the engine means auditing what it now gets wrong"), and .claude/rules/crate-layout.md's note that ranting_gaps/src/english.rs deliberately keeps its own copy of these rules -- do not route that copy through ranting and do not delete it. The cause is compound_plural at src/language/plurals.rs:121, which splits on '-' only and returns None for anything without a hyphen. Extend it to also split on a single space, but ONLY when the second word is in the existing closed PREPOSITIONS or POSTPOSED_ADJECTIVES lists -- an ordinary modifier + head phrase such as "red house" must keep pluralizing on the tail ("red houses"), and that is the risk the closed lists exist to bound. Keep the rebuilt string's original separator (space stays a space, hyphen stays a hyphen). Audit what the new rule now gets wrong and record it: add tests for at least "red house", "post office" and "fire engine" asserting they still pluralize on the tail. This IS a breaking change to rendered English output: say so explicitly in a CHANGELOG.md entry under a Breaking heading. Add integration tests in tests/ranting/regular_plurals.rs, and mark section 1.10 fixed in docs/architecture-review-2026-08-15.md and in ROADMAP.md Phase 8 item 6. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml (., ranting_core, ranting_derive, ranting_i18n, ranting_es, ranting_ar, ranting_ja, ranting_gaps, ranting_es_gaps) as CLAUDE.md requires.
Implement ROADMAP.md Phase 8 item 2, the subjunctive escape hatch, which also fixes defect docs/architecture-review-2026-08-15.md section 1.5: IrregularPluralVerb::Were maps to Some("was") in both first_person (src/language/english.rs:75) and third_person (:87), so say!("If {=i were} rich") renders "If I was rich" and there is no way for a caller to keep a correctly written counterfactual. The indicative mapping is right and is pinned by the regression tuple (IrregularPluralVerb::Were, Some("was"), Some("was")) at src/language/english.rs:555, so it must not simply be deleted; indicative-versus-subjunctive is a property of the caller's clause and is not recoverable from the verb. Implement the escape hatch the ROADMAP item names: a verbatim marker meaning "this verb form is final, inflect nothing", which is general beyond the subjunctive. Read .claude/rules/placeholder-grammar.md before touching the grammar -- pick a marker character that does not collide with any existing case, tense, degree or fused marker, add it to ph_ext's hand-written parser AND keep PH_EXT's regex in parity with the English pass as that file requires (assert_parity must still pass), thread it through ranting_core::placeholder::PlaceholderSpec, and make handle_placeholder skip verb inflection when it is set. Every existing template must render byte-identically -- this is purely additive. Document the marker in README.md's placeholder grammar table, docs/CHEATSHEET.md and docs/API.md, add integration tests in a new tests/ranting/ module registered in tests/ranting/main.rs (subjunctive "were" in first and third person, plus a non-verb control), add a CHANGELOG.md entry under Added, and mark section 1.5 addressed in docs/architecture-review-2026-08-15.md and Phase 8 item 2 done in ROADMAP.md. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml (., ranting_core, ranting_derive, ranting_i18n, ranting_es, ranting_ar, ranting_ja, ranting_gaps, ranting_es_gaps) as CLAUDE.md requires.
Implement ROADMAP.md Phase 8 item 4, ordinals: the #var placeholder spells cardinals only, so "the third attempt" cannot come out of a placeholder. Add an ordinal numeral channel -- a variant of the existing # spelled-out numeral marker -- that renders first, second, third, twenty-first and so on. Read .claude/rules/placeholder-grammar.md and .claude/rules/extension-hooks.md's inflect_numeral_custom paragraph first: the ordinal must arrive at inflect_numeral_custom through NumeralStyle (add a variant) carrying the same real count the cardinal channel already carries, so that ranting_es and ranting_ar can override it -- that second constituency is half the point of the item, since ordinals agree in gender in both languages and this hook surface is on the never-exercised list in docs/architecture-review-2026-08-15.md section 4.1. Adding a NumeralStyle variant is a semver-visible change to a public enum; note that in the CHANGELOG entry. Every existing template must render byte-identically. Document the marker in README.md's placeholder grammar table, docs/CHEATSHEET.md and docs/API.md, add integration tests in tests/ranting/numeral.rs (including a fork-style custom hook that overrides the ordinal style, following the pattern in tests/ranting/custom_inflection.rs), add a CHANGELOG.md entry under Added, and mark Phase 8 item 4 done in ROADMAP.md. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml (., ranting_core, ranting_derive, ranting_i18n, ranting_es, ranting_ar, ranting_ja, ranting_gaps, ranting_es_gaps) as CLAUDE.md requires.
Address defect docs/architecture-review-2026-08-15.md section 1.8 (ROADMAP.md Phase 8 item 6): say!("{=0 walking}") renders the ungrammatical "She walking" silently, because inflect_verb's detect_tense branch correctly returns a non-present verb form untouched but nothing supplied the auxiliary. There is nothing to fix at runtime -- the intended spellings are {=0 walk} for the present and {=0 =walk} for the progressive -- so what is missing is a compile-time diagnostic. The macro has the template string, so in ranting_derive detect a verb-slot word that is a bare -ing or -ed form written with no tense marker, and reject it through the existing StrLitSlice::error path with a message that names both intended spellings. Read .claude/rules/placeholder-grammar.md's check_ident_path section first: this repo has no trybuild compile-fail harness, so pin the new guard by unit-testing the guard function directly rather than the rendered message, exactly as check_ident_path is pinned. Beware two things: tests/ranting/verb_tense.rs::test_continuous_form_walking currently PINS the "She walking" output and will no longer compile once the template is rejected, so retire or rewrite that test in the same change and say in its place why the behavior became a compile error; and the guard must not reject legitimate past-tense verb slots such as {<0 walked} or words that merely end in -ing or -ed without being verb forms in that slot. Add a CHANGELOG.md entry under Changed noting that a previously-accepted malformed template is now a compile error, and mark section 1.8 fixed in docs/architecture-review-2026-08-15.md and in ROADMAP.md Phase 8 item 6. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml (., ranting_core, ranting_derive, ranting_i18n, ranting_es, ranting_ar, ranting_ja, ranting_gaps, ranting_es_gaps) as CLAUDE.md requires.
Implement ROADMAP.md Phase 8 item 1, the participle channel and passive voice: ranting_core::verb_conjugate::to_past_participle already exists and is already fed by the irregular tables, but the only markers that reach it are % (present perfect) and <% (past perfect), so a template cannot compose an auxiliary plus a participle for a variable verb -- passive voice ("the sword is taken"), future perfect ("will have taken") and perfect progressive ("has been picking") are all hand-written today. Read .claude/rules/placeholder-grammar.md in full first, especially the fused-marker section: the shape the ROADMAP item proposes is a fused marker in the family the *= and *@ work established, for instance =% rendering "is"/"are" plus the participle with the auxiliary agreeing in number, and >% rendering "will have" plus the participle. Implement at least the passive (auxiliary be plus past participle, agreeing in number and person) and the future perfect; treat the perfect progressive as optional and say in the ROADMAP item which of the three you landed. Keep ph_ext and PH_EXT in the parity the rules file requires (assert_parity must still pass), thread the new markers through ranting_core::placeholder::TenseMarker or PostSpec as appropriate, and keep every existing template byte-identical -- this is purely additive. Document the markers in README.md's placeholder grammar table, docs/CHEATSHEET.md, docs/API.md and docs/TUTORIAL.md, add integration tests in a new tests/ranting/ module registered in tests/ranting/main.rs covering regular and irregular verbs in both numbers, add a CHANGELOG.md entry under Added, and mark Phase 8 item 1 done in ROADMAP.md. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml (., ranting_core, ranting_derive, ranting_i18n, ranting_es, ranting_ar, ranting_ja, ranting_gaps, ranting_es_gaps) as CLAUDE.md requires.
Implement ROADMAP.md Phase 8 item 3 part (a) only, agreeing quantifiers: ArticleOrSo stops at a/an/some/the/these/those, so no, every, each, either, much/many and less/fewer have no channel and a quantified noun phrase has to be hand-assembled. Add the quantifiers that agree in number, using the machinery the these/those to this/that pair already uses -- "no" is number-transparent ("no item" / "no items"), "every" swaps to "all" on the plural, "each" and "either" force the singular. Do NOT implement part (b), the mass/count flag on the entity: it is a separate attribute-level change and this task is scoped to the article side only; say in the ROADMAP item that (b) remains open. Read .claude/rules/placeholder-grammar.md and .claude/rules/extension-hooks.md's inflect_article_custom paragraph first -- each new word must reach inflect_article_custom with the same GrammaticalCase and count signals the existing articles carry, so a fork can override it, and ArticleKind is a public enum whose new variants are semver-visible: note that in the CHANGELOG entry. Every existing template must render byte-identically. Note also that the zero-count idiom "there are no items" is expressible today as {?#n +items} but is undiscoverable; add a docs/COOKBOOK.md recipe showing the new "no" quantifier as its ergonomic replacement. Document the words in README.md's placeholder grammar table, docs/CHEATSHEET.md and docs/API.md, add integration tests in a new tests/ranting/ module registered in tests/ranting/main.rs, add a CHANGELOG.md entry under Added, and mark Phase 8 item 3 part (a) done in ROADMAP.md. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml (., ranting_core, ranting_derive, ranting_i18n, ranting_es, ranting_ar, ranting_ja, ranting_gaps, ranting_es_gaps) as CLAUDE.md requires.
Decide ROADMAP.md Phase 8 item 5, adverb derivation, in writing rather than by implementing it: the item is scoped to be decided and possibly declined. Write a short design spike document at docs/superpowers/specs/2026-08-15-adverb-derivation.md in the shape of the existing spikes in that directory (state the question, the options including the rejected ones, and a recommendation), answering whether a quick-to-quickly adverb channel is worth adding. The decisive question is reachability, not morphology: the ! and !! degree slot is post-noun only, so establish concretely which sentence positions an English adverb actually occupies in a say!() template, how many of them a placeholder could reach without moving words, and whether the answer is the same shape as ranting_i18n's prenominal-adjective hole -- a channel that exists but that a correct sentence cannot reach. Read .claude/rules/extension-hooks.md's word-order boundary section and docs/EXTENSIBILITY.md section 2.12 first, and cite the locked decision rather than re-opening it. Include what the -ly derivation rules would have to be if it were built (happy to happily, basic to basically, true to truly) and note that they are orthographic, so they would belong with the plural rules in src/language/plurals.rs's spirit rather than in a lexicon. Change no code. Record the recommendation in ROADMAP.md Phase 8 item 5 and link the spike from it. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml (., ranting_core, ranting_derive, ranting_i18n, ranting_es, ranting_ar, ranting_ja, ranting_gaps, ranting_es_gaps) as CLAUDE.md requires.
