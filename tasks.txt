Write a design spike, changing NO code, for ROADMAP.md Phase 8 item 4, ordinals, at docs/superpowers/specs/2026-08-15-ordinal-numerals.md, in the shape of the existing spikes in that directory. The gap: the #var placeholder spells cardinals only, so "the third attempt" cannot come out of a placeholder. The spike must answer: what the ordinal channel's syntax should be, surveying which marker characters are already taken (enumerate them from ranting_core/src/grammar.rs and ph_ext.rs, and read .claude/rules/placeholder-grammar.md in full) and whether an ordinal is better spelled as a variant of the existing # marker or as its own character; how it reaches inflect_numeral_custom -- the recommendation should be a new NumeralStyle variant carrying the same real count the cardinal channel already carries, and the spike must state plainly that adding a variant to that public enum is semver-visible and say what it breaks for downstream matches; what the English -st/-nd/-rd/-th rules are, including the teens exception; and what the second constituency buys, namely that ordinals agree in gender in Spanish and Arabic, so ranting_es and ranting_ar would gain something to override on the never-exercised hook surface that docs/architecture-review-2026-08-15.md section 4.1 records. Read .claude/rules/extension-hooks.md's inflect_numeral_custom paragraph first. Because the sigil grammar is marked Locked in CLAUDE.md and NumeralStyle is public, recommend but DO NOT implement -- add a clearly marked PROPOSED section to ROADMAP.md Phase 8 item 4, link the spike from it, and leave it for a maintainer decision. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml (., ranting_core, ranting_derive, ranting_i18n, ranting_es, ranting_ar, ranting_ja, ranting_gaps, ranting_es_gaps) as CLAUDE.md requires.
Write a design spike, changing NO code, for ROADMAP.md Phase 8 item 1, the participle channel and passive voice, at docs/superpowers/specs/2026-08-15-participle-channel.md, in the shape of the existing spikes in that directory. The gap: ranting_core::verb_conjugate::to_past_participle already exists and is already fed by the irregular tables, but the only markers reaching it are % (present perfect) and <% (past perfect), so a template cannot compose an auxiliary plus a participle for a variable verb -- passive voice ("the sword is taken"), future perfect ("will have taken") and perfect progressive ("has been picking") are all hand-written today. The spike must answer: which fused marker spellings the three constructions should get, in the family the *= and *@ work established (read .claude/rules/placeholder-grammar.md in full, especially the fused-marker section, and enumerate the already-taken characters from ranting_core/src/grammar.rs and ph_ext.rs rather than from memory); whether the passive's auxiliary agreement can reuse the existing AuxiliaryVerb machinery in src/language/auxiliary.rs unchanged; whether the new markers belong in ranting_core::placeholder::TenseMarker or in PostSpec, with the consequence for the macro-runtime seam either way; what keeping PH_EXT and ph_ext in the required parity costs; and which of the three constructions are worth landing together versus separately, ranked by how often ordinary prose needs them. Note explicitly whether any of it approaches the locked word-order boundary and cite that decision rather than re-opening it. Because the sigil grammar is marked Locked in CLAUDE.md, recommend spellings but DO NOT implement them -- add a clearly marked PROPOSED section to ROADMAP.md Phase 8 item 1, link the spike from it, and leave it for a maintainer decision. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml (., ranting_core, ranting_derive, ranting_i18n, ranting_es, ranting_ar, ranting_ja, ranting_gaps, ranting_es_gaps) as CLAUDE.md requires.
Write a design spike, changing NO code, for ROADMAP.md Phase 8 item 3, agreeing quantifiers and the mass/count distinction, at docs/superpowers/specs/2026-08-15-quantifier-determiners.md, in the shape of the existing spikes in that directory. The gap: ArticleOrSo stops at a/an/some/the/these/those, so no, every, each, either, much/many and less/fewer have no channel and a quantified noun phrase is hand-assembled; and with no mass/count flag on the entity, {a 0} on "information" renders "an information" and nothing can pick much over many. The spike must answer, keeping the two parts separate: for part (a), which quantifier words earn a slot, how each behaves on number ("no" is number-transparent, "every" swaps to "all" on the plural, "each" and "either" force the singular), whether they belong in ArticleOrSo or in a sibling type, and what adding variants to the public ArticleKind enum breaks for downstream exhaustive matches; for part (b), whether mass/count belongs as a #[ranting(mass)] attribute in the shape gender already uses or as a trait method, and why (a) is only correct once (b) exists. Read .claude/rules/placeholder-grammar.md and .claude/rules/extension-hooks.md's inflect_article_custom paragraph first -- every proposed word must reach that hook with the same GrammaticalCase and count signals the existing articles carry, or a fork cannot override it, and the spike must say how. Note that the zero-count idiom "there are no items" is expressible today as {?#n +items} but is undiscoverable, and that part (a) is its ergonomic replacement. Because ArticleKind is public and the grammar is Locked in CLAUDE.md, recommend but DO NOT implement -- add a clearly marked PROPOSED section to ROADMAP.md Phase 8 item 3, link the spike from it, and leave it for a maintainer decision. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml (., ranting_core, ranting_derive, ranting_i18n, ranting_es, ranting_ar, ranting_ja, ranting_gaps, ranting_es_gaps) as CLAUDE.md requires.
Decide ROADMAP.md Phase 8 item 5, adverb derivation, in writing rather than by implementing it: the item is scoped to be decided and possibly declined. Write a short design spike at docs/superpowers/specs/2026-08-15-adverb-derivation.md in the shape of the existing spikes in that directory (state the question, the options including the rejected ones, and a recommendation), answering whether a quick-to-quickly adverb channel is worth adding. The decisive question is reachability, not morphology: the ! and !! degree slot is post-noun only, so establish concretely which sentence positions an English adverb actually occupies in a say!() template, how many of them a placeholder could reach without moving words, and whether the answer is the same shape as ranting_i18n's prenominal-adjective hole -- a channel that exists but that a correct sentence cannot reach. Read .claude/rules/extension-hooks.md's word-order boundary section and docs/EXTENSIBILITY.md section 2.12 first, and cite the locked decision rather than re-opening it. Include what the -ly derivation rules would have to be if it were built (happy to happily, basic to basically, true to truly) and note that they are orthographic, so they would belong with the plural rules in src/language/plurals.rs's spirit rather than in a lexicon. Change no code. Record the recommendation in ROADMAP.md Phase 8 item 5 and link the spike from it. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml (., ranting_core, ranting_derive, ranting_i18n, ranting_es, ranting_ar, ranting_ja, ranting_gaps, ranting_es_gaps) as CLAUDE.md requires.
