Implement Phase 3 item 5 "Reflexive Forms" from ROADMAP.md: support myself, yourself, himself, herself, itself, ourselves, themselves, integrated with the existing case marker system (e.g. a marker on {~person do} style placeholders per README.md's placeholder grammar). Follow the exhaustive-match, no-wildcards pattern used elsewhere in src/language/english.rs for pronoun tables. Add integration tests in tests/ranting/. Mark the item's checkbox complete in ROADMAP.md when done.
Implement Phase 3 item 6 "Comparative & Superlative Adjectives" from ROADMAP.md: handle degree (good -> better -> best, bad -> worse -> worst) via marker-based placeholder syntax (e.g. {+person good} for comparative). Follow README.md's placeholder grammar conventions and the codegen-from-data-file pattern used for irregular verbs/plurals if an irregular adjective table is needed. Add integration tests in tests/ranting/. Mark the item's checkbox complete in ROADMAP.md when done.
Implement Phase 3 item 7 "Recursive Type Inflection" from ROADMAP.md: support collections and nested Ranting types such as Vec<Item> where Item: Ranting, Option<Person>, and Box<Noun>, generated via #[derive(Ranting)]. Add integration tests in tests/ranting/. Mark the item's checkbox complete in ROADMAP.md when done.
Implement Phase 4 item 1 "Extract ranting_core shared crate" from ROADMAP.md: create a new plain rlib crate ranting_core that both ranting and ranting_derive depend on (the serde/serde_derive pattern), moving in grammar tables + codegen from data/*.txt, the verb conjugation engine, pronoun logic, and placeholder grammar (PH_START/PH_EXT). Delete both build.rs copy steps, the ranting_derive/data/ symlink fallbacks, and the dual-strum (0.24 + 0.27) compilation constraint described in CLAUDE.md. Update CLAUDE.md's "Architecture" section to describe the new structure. Run the full test suite to confirm zero breaking changes. Mark the item's checkbox complete in ROADMAP.md when done.
Implement Phase 4 item 2 "Dependency modernization" from ROADMAP.md for ranting_derive: upgrade syn 1 to syn 2, darling 0.14 to 0.20+, unify strum on 0.27 (or hand-write SubjectPronoun parsing and drop strum from the derive path). Drop proc-macro-error in favor of plain syn::Error diagnostics. Drop lazy_static in both crates in favor of std::sync::LazyLock/OnceLock. Bump the derive-side regex 1.6 to 1.11. Unify editions to 2024 after the syn 2 migration. Run cargo audit/cargo deny if available to confirm RUSTSEC-clean. Mark the item's checkbox complete in ROADMAP.md when done.
Implement Phase 4 item 3 "Typed placeholder spec across the compile-time/runtime seam" from ROADMAP.md: replace handle_placeholder(..., caps: [&str; 5]) and the "~TENSE~MARKER:WORD" string sentinel with a typed struct/enum (e.g. PlaceholderSpec { article, pre_verb, case, tense, ... }) baked by the macro as a const. Delete the runtime re-parsing (strip_prefix("~TENSE~"), split_once(':'), fallback branches for states the macro can never produce) and the runtime re-recognition of articles by string-matching "the"/"a"/"some" in get_article_or_so. Document or disambiguate the overloaded '=' marker (subject case before the noun vs. continuous tense after it) while this lands. Run the full test suite. Mark the item's checkbox complete in ROADMAP.md when done.
Implement Phase 4 item 4 "Type the subject; remove runtime panics" from ROADMAP.md: make SubjectPronoun public with a FromStr impl and store it in Noun, so invalid subjects are unrepresentable instead of Noun::new panicking via assert!. Replace is_subjective_plural's discriminant comparison (as usize >= 6) with an explicit match. Make remaining fallible paths (inflect() on non-singularizable plurals, expect in is_subjective_plural) degrade gracefully instead of panicking. Run the full test suite. Mark the item's checkbox complete in ROADMAP.md when done.
Implement Phase 4 item 5 "Public API cleanup" from ROADMAP.md: rename inflect_possesive to inflect_possessive (public API, fix the typo). Add #[doc(hidden)] to macro plumbing (handle_placeholder, handle_placeholder_with_context, handle_tense_marker), matching HeedMatcher's existing treatment. Rework ack!()/nay!() to expand to expression forms (Ok(say!(...))/Err(say!(...))) instead of a hidden return, updating callers to write return themselves. Run the full test suite and update docs/examples referencing the old names. Mark the item's checkbox complete in ROADMAP.md when done.
Implement Phase 4 item 6 "Hand-written placeholder tokenizer" from ROADMAP.md: replace the PH_EXT regex internals with a small hand-written tokenizer that recognizes placeholder internals (keep the outer sigil grammar unchanged - it's the crate's identity). Produce precise error spans (e.g. "expected article or verb, found ...") instead of the blanket "Error in placeholder" message. Run the full test suite, including error-message assertions. Mark the item's checkbox complete in ROADMAP.md when done.
