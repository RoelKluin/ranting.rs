Address defect docs/architecture-review-2026-08-15.md section 1.8 (ROADMAP.md Phase 8 item 6): say!("{=0 walking}") renders the ungrammatical "She walking" silently, because inflect_verb's detect_tense branch correctly returns a non-present verb form untouched but nothing supplied the auxiliary. There is nothing to fix at runtime -- the intended spellings are {=0 walk} for the present and {=0 =walk} for the progressive -- so what is missing is a compile-time diagnostic. The macro has the template string, so in ranting_derive detect a verb-slot word that is a bare -ing or -ed form written with no tense marker, and reject it through the existing StrLitSlice::error path with a message that names both intended spellings. Read .claude/rules/placeholder-grammar.md's check_ident_path section first: this repo has no trybuild compile-fail harness, so pin the new guard by unit-testing the guard function directly rather than the rendered message, exactly as check_ident_path is pinned. Beware three things: tests/ranting/verb_tense.rs::test_continuous_form_walking currently PINS the "She walking" output and will no longer compile once the template is rejected, so retire or rewrite that test in the same change and say in its place why the behavior became a compile error; the guard must not reject legitimate past-tense verb slots such as {<0 walked} or words that merely end in -ing or -ed without being verb forms in that slot; and before implementing, grep README.md, docs/TUTORIAL.md, docs/COOKBOOK.md, docs/CHEATSHEET.md, docs/API.md and all doctests for any other template of that shape, since a doctest using one will stop compiling too. Add a CHANGELOG.md entry under Changed noting that a previously-accepted malformed template is now a compile error, and mark section 1.8 fixed in docs/architecture-review-2026-08-15.md and in ROADMAP.md Phase 8 item 6. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml (., ranting_core, ranting_derive, ranting_i18n, ranting_es, ranting_ar, ranting_ja, ranting_gaps, ranting_es_gaps) as CLAUDE.md requires.
