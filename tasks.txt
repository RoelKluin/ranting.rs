Write a design spike, changing NO code, for ROADMAP.md Phase 9 item 3, derive-generated inflect() against non-English input. Read ROADMAP.md's Phase 9 section (item 3), docs/architecture-review-2026-08-14.md section 4.7 in full, and .claude/rules/pluralization.md's 'Blind spot worth knowing' note. Confirm the current state: both ranting_i18n and ranting_es hand-write their Ranting::inflect() implementations rather than relying on #[derive_ranting]'s generated fallback, so nothing in this repo exercises the derive-generated inflect() (ranting_derive/src/ranting_impl.rs's get_plurality_fns fallback sites, which call ranting::inflect_noun_regular) against genuinely non-English nouns -- six green gates exclude this path entirely. Evaluate the two candidate shapes the item names: (a) a fifth, deliberately minimal falsifier crate that leans on the derive default rather than a hand-written impl, versus (b) extending an existing falsifier's lexicon (ranting_i18n or ranting_es) with entries that go through the default derive path instead of an override, e.g. by not setting singular_end/plural_end and not implementing inflect() by hand for a subset of nouns. Recommend one, considering the falsifier-contract rules in .claude/rules/crate-layout.md (a new falsifier must still depend on ranting alone, never ranting_core/ranting_derive) and what it would actually prove -- English orthographic pluralization rules (src/language/plurals.rs's regular_plural/compound_plural) applied to German or Spanish nouns will very likely mangle them (e.g. English -s/-es suffixing on a German or Spanish noun that needs actual umlaut/vowel changes or a different ending), so state plainly whether you expect this to surface a real gap or confirm one already implicitly known. Write the spike as docs/superpowers/specs/2026-08-18-derive-inflect-non-english.md, following the shape of the existing specs in that directory (e.g. docs/superpowers/specs/2026-08-17-hook-falsification-depth.md). Do NOT touch any .rs file, Cargo.toml, or existing test -- this is a spike only, though you may compile-check a throwaway scratch fixture outside the repo tree the way the item-1 spike did, as long as nothing from it is left in the tree. Update ROADMAP.md Phase 9 item 3 with a short 'PROPOSED (2026-08-18 spike -- NOT implemented; maintainer decision needed)' paragraph pointing at the new spec, mirroring items 1 and 2's own PROPOSED blocks. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml as CLAUDE.md requires (this should be a no-op gate pass since no .rs/.toml file changes).
