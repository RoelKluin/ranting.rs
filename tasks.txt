Implement Phase 6 item 23 "Spanish reference lexicon" from ROADMAP.md: the second acceptance test, run it after the items above. Create a new crate directory ranting_es/ (this repo is NOT a cargo workspace - each crate has its own Cargo.lock - so give it its own Cargo.toml depending on ranting = { path = ".." }), following ranting_i18n/ as the model for structure, README and falsification contract. Read ranting_i18n/README.md first, and docs/EXTENSIBILITY.md section 4.3 which already carries a complete Spanish implementation to build from. Implement Spanish for a deliberately small closed vocabulary using only ranting's public API: el/la/los/las and un/una definite and indefinite articles via NounClass, post-nominal adjective agreement (el gato negro, la casa negra, los gatos negros), verb agreement, tu and usted second-person forms, and the numerals 0 to 12. Spanish is the acceptance test German could not be for adjectives: item 10 found German attributive adjectives are prenominal while the degree slot is post-noun, so no German sentence renders the adjective hook correctly, whereas Spanish adjectives are post-nominal and can exercise it end to end. Same falsification contract as item 10: every place Spanish cannot reach something through the public API is a hole, written down in the crate's README with a test pinning it, never worked around; and cross-reference each hole to the ROADMAP item it belongs to. Keep cargo fmt --check, cargo clippy -- -D warnings and cargo test green in the new crate and at the repo root. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 24 "Fold the case-inventory spike into the reference docs" from ROADMAP.md: this is follow-ups 2 and 3 of the recommendation in docs/superpowers/specs/2026-08-13-grammatical-case-inventory.md, which item 18 wrote and which concluded "change nothing, document it" - the spec is written but the two documentation changes it authorizes were not made. First, fold the finding into docs/EXTENSIBILITY.md as a subsection under GrammaticalCase, explaining what the enum's five variants scope to and pointing at GermanNoun::in_case in ranting_i18n as the worked pattern for a case the marker set cannot name. Second, add a row for GrammaticalCase to the Key Architecture Decisions table in ROADMAP.md - it has none today - stating that it is locked at English's five-marker inventory and pointing at that spec, mirroring the shape of the existing SubjectPronoun row. Do not change any code or any hook signature: the spike explicitly rejected widening the enum, adding markers, and an open string-typed case channel, and hole 3 in ranting_i18n/README.md stays open and stays pinned by its hole_3 test. Mark item 24 done in ROADMAP.md by inserting a checkmark before its bold title with implementation notes as sub-bullets.
Implement Phase 6 item 25 "Preposition fusion and the closed pre-noun word list" from ROADMAP.md: this is hole 7 in ranting_i18n/README.md, the last item-10 hole with no queued follow-up. The pre-noun slot in a placeholder accepts only a closed list of English words, so say!("{in the =0}", haus) is a compile error, and preposition-article fusion - German zu+dem=zum and in+dem=im, French de+le=du and a+le=au, Spanish a+el=al, Italian di+il=del - is unreachable. Note that elide_article_custom cannot reach it either, and record why precisely: that hook runs after assembly on the article, separator and following text, but the preposition here is template literal text sitting OUTSIDE the placeholder, so it is never in the spliced span - CLAUDE.md already states this for the French de+le=du case. Write this as a doc-only spike in docs/superpowers/specs/ dated 2026-08-13, in the same shape as the grammatical-case-inventory and number-categories specs: state the problem, enumerate options including opening the pre-noun slot in ranting_derive's parser versus a new hook versus leaving it to the caller's template, name what stays out of reach under each, give a recommendation, and record rejected alternatives. Do not change code. Mark item 25 done in ROADMAP.md by inserting a checkmark before its bold title with the outcome as sub-bullets.
Implement Phase 6 item 26 "Adjective declension class is not reported" from ROADMAP.md: this is the second half of hole 4 in ranting_i18n/README.md - the half that is NOT about word order, so do not conflate it with the prenominal-position problem that item 20 documents as the caller's-template boundary. German adjective endings depend on which article preceded the adjective: strong declension after no article, weak after a definite article, mixed after an indefinite one, so ein guter Hund versus der gute Hund versus guter Hund all differ in the adjective ending alone. inflect_adjective_custom receives the degree, the NounClass, the plural flag and now the count, but never learns which article was just rendered in the same placeholder, so a fork cannot pick the ending. Write this as a doc-only spike in docs/superpowers/specs/ dated 2026-08-13 in the same shape as the other Phase 6 spikes: state the problem with worked German examples, enumerate the options - passing the rendered article to the adjective hook, an ArticleKind-shaped parameter mirroring ranting_core, or entity-carried state as GermanNoun::in_case already does - name what stays out of reach under each, recommend one, and record rejected alternatives. Note explicitly whether your recommendation would be another hook-signature break and if so say that it should be bundled with any other owed break rather than taken alone. Do not change code. Mark item 26 done in ROADMAP.md by inserting a checkmark before its bold title with the outcome as sub-bullets.
Harden scripts/overnight_loop.sh against self-modification. Tonight item 13 edited that script while bash was executing it, which had two bad consequences: the improvement did not take effect for the rest of the run at all, because bash had already parsed the whole while-loop compound command before the edit, so the run kept using the old root-only gate and consequently committed a change in item 14 that broke the ranting_i18n sibling crate; and because the file grew by about 1.5 kilobytes ABOVE the loop, the byte offset bash resumes from after the loop ends now points into the middle of the loop body, so the end-of-run cleanup and summary can misparse. Fix this by making the script, at startup, copy itself to a mktemp path and exec that copy with the original arguments, guarded by an environment variable so the re-exec happens exactly once and not recursively, and arrange for the temp copy to be removed on exit with a trap. Add a comment block explaining the failure mode above so nobody undoes it later. Verify with a test harness under a temporary directory using PATH-stubbed cargo and claude binaries, in the style already used this session, that a task which rewrites scripts/overnight_loop.sh mid-run neither changes the running behavior nor corrupts the end-of-run summary, and that the edited version is what a subsequent run uses. Do not change the gate or queue semantics while doing this.
Implement Phase 6 item 27 "Document the template-selection scaling cost" from ROADMAP.md: this is recommendation point 2 of docs/superpowers/specs/2026-08-13-template-selection.md, which item 22 wrote and which concluded "leave it to the caller, documented" - the spec exists but the documentation change it authorizes was not made. Add it to docs/EXTENSIBILITY.md in the same section area where item 20 documented the word-order boundary, since the two are the same boundary seen from different sides. State plainly that selecting a per-language template costs languages times sentences of source text, that no crate-level indirection reduces that number because each language's placeholder inflection genuinely needs its own compile-time-parsed literal, and that the only thing any design changes is whose hand writes the multiplication. Explain the underlying constraint concretely: say!() parses its first argument as a syn::LitStr at macro-expansion time, before any runtime value exists, so passing a HashMap lookup or a function call or a match-returning-a-string as the template is a compile error rather than a slow path - and note that heed!() and ask!() share this constraint through compile_heed_template. Record that a sugar macro over the per-language match is explicitly NOT ruled out as a future ergonomics-only addition, just unscheduled until a downstream fork demonstrates the pain. Do not add any macro, type, or other crate surface. Mark item 27 done in ROADMAP.md by inserting a checkmark before its bold title with implementation notes as sub-bullets.
Audit the prose documentation against the code that Phase 6 items 14 through 21 actually changed, and fix every drift you find. Several hook signatures and behaviors changed during this run and the documentation was updated by each item only where that item happened to look: item 14 added an Option<PlaceholderCount> parameter to the twelve _custom and _with_context hook methods and a case parameter to Ranting::inflect; item 15 made Many expose its length as the placeholder count; item 16 made is_first_person_subject an overridable hook with wrapper delegation; item 17 changed sentence detection beyond Latin punctuation; item 19 split case marking from pronoun display and introduced the fused *= and *@ placeholder marker forms that reach inflect() with a real GrammaticalCase. Check CLAUDE.md, README.md, docs/API.md, docs/EXTENSIBILITY.md, docs/CHEATSHEET.md, docs/TUTORIAL.md and docs/COOKBOOK.md against the current code, and correct any signature, parameter list, marker table, or behavioral claim that no longer matches - in particular any place that still says a hook takes as_plural alone, any marker table that omits the fused forms, and CLAUDE.md's long non-obvious-behaviors bullets which state parameter lists explicitly and state method counts that go stale. Do not change code to match docs; the code is the truth here. Where a documented claim is now simply wrong rather than incomplete, say so in the commit message.
Write a CHANGELOG.md entry for v1.3.0 covering all of Phase 6, creating the file if the repo has none. Read ROADMAP.md Phase 6 and DONE.md for the item list and git log for what actually landed. Organize it for a reader of the crate rather than a reader of the roadmap: lead with the user-visible additions - the six _custom hook pairs and their _with_context twins, NounClass, GrammaticalCase and its fused *= and *@ marker forms, AdjectiveDegree, OrthographyRole, NumeralStyle, the capitalize and elide_article_custom and inflect_numeral_custom hooks, is_first_person_subject becoming overridable, Many exposing its length as a count, sentence detection beyond Latin punctuation, and the say_with! and derive_ranting re-exports - then the breaking changes, clearly marked as breaking with the old and new signatures shown side by side, since every _custom hook override in downstream code needs editing. Add a short section pointing at docs/EXTENSIBILITY.md for forks and at the docs/superpowers/specs/ spikes for the decisions that were deliberately NOT taken, naming the main ones: word order stays in the caller's template, GrammaticalCase stays at English's five-marker inventory, per-language template selection stays caller-side, and whitespace stays heed!()'s only word boundary. Do not overstate: several Phase 6 items were doc-only spikes that changed no code, and the ranting_i18n falsifier crate still records open holes - link that README rather than implying the phase closed everything.
