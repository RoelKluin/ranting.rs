Implement Phase 6 item 11 "Zero-length article still emits its separator" from ROADMAP.md: this is a real bug producing user-visible wrong output, found by item 10's German lexicon (hole 6, see ranting_i18n/README.md and its tests/holes.rs). When inflect_article_custom returns an empty string to mean "no article here" - which German needs for the indefinite plural, Hunde bellen - the separator between article and noun is emitted anyway, so the placeholder renders with a leading space and a doubled space mid-sentence. Read ranting_i18n/tests/holes.rs's hole_6 test first; it pins the current wrong behavior with a probe. Fix handle_placeholder_impl in src/lib.rs so a zero-length article emits no separator. Note that elide_article_custom cannot repair this - the post-assembly splice is skipped when the recorded article span is empty, so the hook is never called - and that skip_article() is the wrong tool because it is per-entity and unconditional, so it cannot mean "no article in the plural only" and would swallow der/die/das too. Existing English say!() output must be unchanged. Update the hole_6 test in ranting_i18n to assert the corrected output and strike the entry from that README, add integration tests in tests/ranting/, and run the gate inside ranting_i18n as well as at the repo root since the root cargo test does not compile that crate. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 19 "Case marking and pronoun display share one hook" from ROADMAP.md: found by item 10 as hole 5 (see ranting_i18n/README.md and hole_5 in its tests/holes.rs). A case marker does two jobs at once - it tells inflect_article_custom the noun's grammatical role and it switches the noun slot from the name to a pronoun - so a fork that overrides inflect_pronoun_custom to return the noun's name, which tests/ranting/grammatical_case.rs demonstrates and describes as what a case-declining fork typically does, thereby loses real pronouns for that entity: say!("Ich sehe {@0}.", hund) renders "Ich sehe Hund." instead of "Ich sehe ihn." Provide a way to say "case-mark this placeholder but render the name", separating the two jobs. Prefer a solution that does not add a new placeholder marker if one exists, since the marker set is a ✅ Locked architecture decision; if a new marker is genuinely required, record the rejected alternatives in the ROADMAP item's sub-bullets. Existing say!() output must be unchanged. Add integration tests in tests/ranting/, update ranting_i18n to drop its Render::Name workaround and strike hole 5 from its README, document in docs/EXTENSIBILITY.md, and run the gate in ranting_i18n as well as at the root. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 20 "Document the word-order boundary" from ROADMAP.md: this is open question 1 of docs/superpowers/specs/2026-08-13-word-order-feasibility.md, which recommends the boundary statement land in docs/EXTENSIBILITY.md in full with one-line pointers from CLAUDE.md and README.md. This is a documentation-only task, write no production code. Read that spec first and reuse its own single-sentence version verbatim as the pointer text: ranting inflects words within a template; the order of those words is the template's, and the template is the caller's - so a non-English application needs one template per language, and no inflection hook will ever change that. Write it as a permanent boundary, not a TODO, matching how docs/superpowers/specs/2026-08-12-input-parsing-feasibility.md insisted pronoun reversal be documented as fundamentally ambiguous rather than not-yet-implemented, and include the spec's named list of constructions that stay out of reach (German verb-second with separable prefixes, SOV with postpositions, VSO, suffixed definite articles, adjective movement, sentence-final particles). Add the item 10 finding that German's attributive adjectives are prenominal while the degree slot is post-noun, so there is no German sentence in which the adjective hook's output is correct - it is the sharpest single illustration of the boundary. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 21 "Document non-English pronoun inventories" from ROADMAP.md: this is the entire follow-up that docs/superpowers/specs/2026-08-13-pronoun-inventory.md authorizes - its recommendation is option (c) in thin form, change nothing and document what already exists. This is a documentation-only task, write no production code. Read that spec first. Add a docs/EXTENSIBILITY.md section on non-English pronoun inventories covering: subjective() -> &str being an open uninterpreted channel; the five PronounCase arms plus inflect_verb_custom being the fork's own table; declaring a #[ranting(subject = "...")] struct rather than reusing Noun; the T-V precedence rule (addressee label beats ctx.register beats nothing); and the unwrap_or(It) degrade as the documented consequence of leaving an arm unhandled. Add the item 10 confirmation that an unrecognized subjective() degrades silently - a declined verb takes the catch-all arm of english::inflect_verb and renders the bare form, "Der Hund walk." - since that is the cost of the closed-enum decision made concrete. Then flip the "SubjectPronoun is a closed English enum" row in ROADMAP.md's Key Architecture Decisions table to ✅ Locked, pointing at this spec. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 22 "Per-language template selection spike" from ROADMAP.md: this is a documentation-only task, write no production code. Produce docs/superpowers/specs/2026-08-13-template-selection.md. docs/superpowers/specs/2026-08-13-word-order-feasibility.md recommends per-language template sets and says the caller selects the template before the say!() call, but never addresses how: say!() parses its literal at compile time, so the selection cannot be a runtime lookup in a translation catalogue and must be a literal match lang { Lang::De => say!("..."), Lang::En => say!("...") } written out at every call site, which scales as languages times sentences and is the real ergonomic cost of that recommendation. Read that spec and ranting_derive/src/lib.rs's say!() codegen first. Score at least: leaving it to the caller and documenting it; a macro that takes a template set and expands the match; and a per-language template-set type declared once with a macro selecting from it by a runtime language value. State for each whether it preserves the ✅ Locked "compile-time parsing + runtime inflection" architecture decision, since anything resolving templates at runtime does not. End with a recommendation and the rejected alternatives with reasons. Concluding "leave it to the caller, documented" is an acceptable outcome. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 23 "Spanish reference lexicon" from ROADMAP.md: the second acceptance test, run it after the items above. Create a new crate directory ranting_es/ (this repo is NOT a cargo workspace - each crate has its own Cargo.lock - so give it its own Cargo.toml depending on ranting = { path = ".." }), following ranting_i18n/ as the model for structure, README and falsification contract. Read ranting_i18n/README.md first, and docs/EXTENSIBILITY.md section 4.3 which already carries a complete Spanish implementation to build from. Implement Spanish for a deliberately small closed vocabulary using only ranting's public API: el/la/los/las and un/una definite and indefinite articles via NounClass, post-nominal adjective agreement (el gato negro, la casa negra, los gatos negros), verb agreement, tu and usted second-person forms, and the numerals 0 to 12. Spanish is the acceptance test German could not be for adjectives: item 10 found German attributive adjectives are prenominal while the degree slot is post-noun, so no German sentence renders the adjective hook correctly, whereas Spanish adjectives are post-nominal and can exercise it end to end. Same falsification contract as item 10: every place Spanish cannot reach something through the public API is a hole, written down in the crate's README with a test pinning it, never worked around; and cross-reference each hole to the ROADMAP item it belongs to. Keep cargo fmt --check, cargo clippy -- -D warnings and cargo test green in the new crate and at the repo root. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
