Write a design spike, changing NO code, for ROADMAP.md Phase 9 item 2, negative-count agreement now unblocked by is_mass(). Read ROADMAP.md's Phase 9 section (item 2) and the two things it cites: ROADMAP.md Phase 8 section 1.12's original recorded text, and docs/architecture-review-2026-08-15.md section 1.12. Confirm the current behavior against the built crate (as_pl = count != Some(1) agreeing plural for every negative count, e.g. '{#n item}' with n = -1 today renders 'minus one items') and confirm Ranting::is_mass() (landed Phase 8 item 3, src/lib.rs) is reachable at the call site that computes as_pl. Propose the exact gating change (something like: agree plural only when !self.is_mass() && count != Some(1), or the precise equivalent given how the numeral/count value flows to that site), state whether it's a breaking change to any existing pinned test output (search tests/ranting/ and docs for 'minus one'), and note any measure-noun vs countable-noun test cases worth adding. Write the spike as docs/superpowers/specs/2026-08-17-negative-count-mass-agreement.md, following the shape of the existing specs in that directory (e.g. docs/superpowers/specs/2026-08-15-quantifier-determiners.md) -- PROPOSED, not decided, no maintainer ruling assumed. Do NOT touch any .rs file, Cargo.toml, or existing test. Update ROADMAP.md Phase 9 item 2 with a short 'PROPOSED (2026-08-17 spike -- NOT implemented; maintainer decision needed)' paragraph pointing at the new spec, mirroring how Phase 8 items recorded their own PROPOSED blocks. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml as CLAUDE.md requires (this should be a no-op gate pass since no .rs/.toml file changes).
Write a design spike, changing NO code, for ROADMAP.md Phase 9 item 1, exercising the nine never-overridden hooks. Read ROADMAP.md's Phase 9 section (item 1) and docs/architecture-review-2026-08-15.md section 4.1 in full, plus .claude/rules/extension-hooks.md's '_with_context mechanism' section. Confirm the current state with scripts/hook_audit.sh (or read it and reproduce its check by hand if it needs updating) run against all four falsifier crates (ranting_i18n, ranting_es, ranting_ar, ranting_ja) plus ranting_gaps, to verify the eight _with_context hook twins plus is_first_person_subject_custom are still never overridden anywhere. Propose ONE concrete falsification target that would give a real fork a reason to override one of these nine hooks with genuinely different behavior from its default -- item 1's own text suggests ranting_ja's register/dialect threading a mid-story register CHANGE (different politeness to two different addressees in one say_with!() sequence) as the most natural candidate, or a first-person label other than 'I'/'we' in some fork's lexicon to exercise is_first_person_subject_custom. State which falsifier crate would carry it, what the new test would assert, and whether it's expected to find a real gap (a hook that can't actually express what's needed) or merely confirm the default is sufficient once actually exercised -- both are valid findings per this repo's own falsifier philosophy. Write the spike as docs/superpowers/specs/2026-08-17-hook-falsification-depth.md, following the shape of existing specs in that directory. Do NOT touch any .rs file, Cargo.toml, or existing test -- this is a spike only. Update ROADMAP.md Phase 9 item 1 with a short 'PROPOSED (2026-08-17 spike -- NOT implemented; maintainer decision needed)' paragraph pointing at the new spec. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml as CLAUDE.md requires (this should be a no-op gate pass since no .rs/.toml file changes).
