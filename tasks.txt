Implement Phase 6 item 1 "Word-order & template-slot design spike" from ROADMAP.md: this is a documentation-only task, write no production code. Produce docs/superpowers/specs/2026-08-13-word-order-feasibility.md in the style of the existing docs/superpowers/specs/2026-08-12-input-parsing-feasibility.md (read it first - it is the precedent for a spike concluding "don't build this"). Enumerate and score at least the three options the ROADMAP item lists (per-language template sets; numbered slots plus per-language reorder metadata; a syntax-tree sentence!() API), grounding each in what handle_placeholder_impl in src/lib.rs can and cannot reach, and note that no inflection hook can move text between placeholders because it does not own that text. End with a recommendation, the rejected alternatives and why, and an explicit list of what stays impossible for ranting-i18n under that recommendation. Concluding "per-language templates, documented as a permanent boundary" is an acceptable outcome. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title, matching the existing "6. ✅ **Comparative & Superlative Adjectives**" shape, and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 2 "Lexical gender / noun-class channel" from ROADMAP.md: give nouns an open-ended noun-class label so a non-English Ranting impl no longer needs an external string-keyed gender table. Read commit 11d531ed first (git show 11d531ed) - the GrammaticalCase change is the exact template to follow. Add a public open-ended class channel (a newtype over &'static str, NOT a closed Masculine/Feminine/Neuter enum - Bantu has a dozen-plus classes and Danish has common/neuter), a Ranting::noun_class() trait method defaulting to unset, a matching #[ranting(gender = "...")] derive attribute in ranting_derive, and thread it into inflect_article_custom/_with_context and inflect_pronoun_custom/_with_context exactly as GrammaticalCase was threaded. The change must be additive: impls that set no class keep byte-identical behavior, verified by the existing test suite passing unchanged. Add integration tests in tests/ranting/ with a worked German example selecting der Hund / die Katze / das Haus from one code path. Document the new hook in docs/EXTENSIBILITY.md and docs/API.md, and add a CLAUDE.md "Non-obvious behaviors" bullet for it. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title, matching the existing "6. ✅ **Comparative & Superlative Adjectives**" shape, and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 3 "Pronoun-inventory & T-V register design spike" from ROADMAP.md: this is a documentation-only task, write no production code. Produce docs/superpowers/specs/2026-08-13-pronoun-inventory.md. Read src/language/english.rs, ranting_core/src/grammar.rs (SubjectPronoun) and src/narration.rs (NarrationContext.register) first. Score the three options the ROADMAP item lists (extend SubjectPronoun with non-English variants; make the pronoun channel open like item 2's noun class; keep SubjectPronoun English-only and add a parallel fork-owned pronoun-set trait consulted first by handle_placeholder_impl) against what each costs in exhaustive-match safety, which the Key Architecture Decisions table calls out as deliberate. Resolve explicitly whether the already-inert NarrationContext.register (story-wide) or a new per-addressee channel owns the T-V distinction (du/Sie, tu/vous), or how they compose. State which option is breaking and for whom. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title, matching the existing "6. ✅ **Comparative & Superlative Adjectives**" shape, and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 4 "Number-category design spike" from ROADMAP.md: this is a documentation-only task, write no production code. Produce docs/superpowers/specs/2026-08-13-number-categories.md. Inventory every place number is a bool in the crate (is_plural, as_plural in all six _custom hooks, inflect(to_plural,..), the +/- markers, #var/$var agreement) by reading src/lib.rs and src/language/, and state exactly which signatures a change would break. Score the three options the ROADMAP item lists (keep the bool and document non-English plural categories as out of scope; add a parallel plural_category() channel with the bool staying authoritative for English; replace the bool with a Number enum in one coordinated breaking release) against Arabic dual, Slavic paucal and CLDR zero/one/two/few/many/other. Whichever is recommended, also add a short paragraph to CLAUDE.md's "Non-obvious behaviors" stating what as_plural: bool does and does not promise to a fork author. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title, matching the existing "6. ✅ **Comparative & Superlative Adjectives**" shape, and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 5 "Adjective-agreement runtime hook" from ROADMAP.md: ranting has no runtime adjective path at all today - degree (!/!!) is baked at compile time in ranting_derive/src/language/adjective.rs - but Romance and Germanic adjectives agree with their noun in gender, number and case, none of which is known at compile time. Add Ranting::inflect_adjective_custom and inflect_adjective_custom_with_context receiving the adjective plus GrammaticalCase, number, and (if Phase 6 item 2 has landed) the noun class, following commit 11d531ed's threading pattern (git show 11d531ed). Call the hook from handle_placeholder_impl in src/lib.rs before the compile-time-baked degree form is emitted, falling back to that baked form when the hook returns None so say!()'s English output is unchanged. Add integration tests in tests/ranting/ with a worked French example rendering un chat noir / une robe noire / des chats noirs from one template. Document the new hook in docs/EXTENSIBILITY.md and docs/API.md, and add a CLAUDE.md "Non-obvious behaviors" bullet for it. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title, matching the existing "6. ✅ **Comparative & Superlative Adjectives**" shape, and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 6 "Orthography & capitalization hook" from ROADMAP.md: uc_1st_if, the sentence-start-uppercase default, the ,/^ markers and apply_case in src/language/plurals.rs are English orthographic assumptions compiled into the crate - German capitalizes every noun regardless of position, Japanese and Arabic have no case so uc: bool is meaningless, and Turkish dotted/dotless i is wrong under char::to_uppercase. Route capitalization through a new Ranting hook that defaults to exactly today's behavior, instead of calling uc_1st_if directly at each site in src/lib.rs, so a language module can make it a no-op or an always-capitalize. Verify the uc plumbing through Many/Maybe/Box in src/collections.rs still behaves (the uppercase-first-char-only join is already tested). Existing say!() output must be unchanged. Add integration tests in tests/ranting/ including an always-capitalize-nouns German-style override. Document the new hook in docs/EXTENSIBILITY.md and docs/API.md, and add a CLAUDE.md "Non-obvious behaviors" bullet for it. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title, matching the existing "6. ✅ **Comparative & Superlative Adjectives**" shape, and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 7 "Phonological elision / contraction hook" from ROADMAP.md: the a/an choice is hard-coded English phonology and the article hook returns a string without ever seeing the word that follows it, so French le+vowel to l', de+le to du, and Italian lo/il/l' are all inexpressible. Give the article hook (or a new post-assembly hook, whichever fits handle_placeholder_impl in src/lib.rs more cleanly - record the choice and the rejected alternative in the ROADMAP item's sub-bullets) access to the following word so a fork can elide or fuse. English a/an must keep working on the existing path with byte-identical output. Add integration tests in tests/ranting/ with a worked French example producing l'homme versus le chien. Document the new hook in docs/EXTENSIBILITY.md and docs/API.md, and add a CLAUDE.md "Non-obvious behaviors" bullet for it. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title, matching the existing "6. ✅ **Comparative & Superlative Adjectives**" shape, and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 8 "Locale-aware numeral rendering" from ROADMAP.md: #var spells a number out in English words and $var renders ASCII digits, both hard-coded, but every other language needs its own speller and several agree the numeral itself with gender and case (Russian dva/dve). Add a numeral hook on the Ranting trait (or a small Numeral trait a language module implements - record the choice and the rejected alternative in the ROADMAP item's sub-bullets), defaulting to the current English speller, and thread it through every place #var and $var are rendered. Existing say!() output must be unchanged. Add integration tests in tests/ranting/ with a non-English speller override for both #var and $var. Document the new hook in docs/EXTENSIBILITY.md and docs/API.md, and add a CLAUDE.md "Non-obvious behaviors" bullet for it. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title, matching the existing "6. ✅ **Comparative & Superlative Adjectives**" shape, and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 9 "Non-space-delimited script support in heed!()/ask!()" from ROADMAP.md: {name} captures one whitespace-delimited token and the regex ranting_derive/src/heed.rs compiles is built on that assumption, so Japanese, Chinese and Thai input returns nothing useful. Decide between a documented permanent restriction to space-delimited scripts (cheap and honest, consistent with docs/superpowers/specs/2026-08-12-input-parsing-feasibility.md declining unbuildable generality) and a pluggable tokenizer boundary in compile_heed_template, and record the decision plus the rejected alternative in the ROADMAP item's sub-bullets. Whichever is chosen, README.md's heed!() section and CLAUDE.md's heed capture-syntax bullet must state the behavior explicitly rather than leave it implied. Add integration tests in tests/ranting/ covering the chosen behavior on non-space-delimited input. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title, matching the existing "6. ✅ **Comparative & Superlative Adjectives**" shape, and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 10 "`ranting-i18n` companion crate — German reference lexicon" from ROADMAP.md: this is the acceptance test for items 1-9, so run it last. This repo is NOT a cargo workspace - ranting, ranting_core and ranting_derive each have their own Cargo.lock - so create the new crate as its own directory ranting_i18n/ (directory name matching ranting_core/ranting_derive; the crate name in Cargo.toml may be ranting-i18n) with its own Cargo.toml depending on ranting = { path = ".." }, exactly as ranting_derive depends on ranting_core. Depend only on ranting's public API - not ranting_core, not any pub(crate) item, and without forking handle_placeholder_impl. Implement German for a deliberately small closed vocabulary: three nouns of differing gender, nominative/accusative/dative, definite and indefinite articles, adjective agreement, and verb agreement. Its purpose is falsification: every place it cannot reach something through the public API is a hole items 1-9 failed to close, and each one must be written down in the new crate's README.md and cross-referenced from the ROADMAP Phase 6 item it belongs to, not worked around. If German still needs per-language templates for word order, say so plainly in that README rather than papering over it. Add integration tests in the new crate and keep cargo fmt --check, cargo clippy -- -D warnings and cargo test green both in the repo root and inside ranting_i18n/. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title, matching the existing "6. ✅ **Comparative & Superlative Adjectives**" shape, and record the implementation notes as sub-bullets under it.
