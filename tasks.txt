Write a design spike, changing NO code, for ROADMAP.md Phase 9 item 5, a ranting-if (or similar) companion crate for Inform7-style object disambiguation. Read ROADMAP.md's Phase 9 section (item 5) and the Post-v1.2 Future Directions entry it points to in full. Read .claude/rules/heed-input-parsing.md (the ask!()/Answerable/heed!() mechanisms this item says it would build on) and src/answerable.rs, plus ranting_gaps/README.md and ranting_es_gaps/README.md as examples of how this repo scopes a new non-core sibling crate. This item is the least-scoped item on the Phase 9 list -- the spike's job is to propose: (a) a minimal, concrete first slice of Inform7 Understand-rulebook-style disambiguation (a candidate registry, a scoring/likelihood mechanism, and a rule-authoring syntax) that would be worth building first, not the whole feature; (b) whether it depends on ranting alone (falsifier-shaped) or needs ranting_core (dev-tool-shaped, like ranting_gaps) and why; (c) how it would integrate with an existing ask!()/Answerable call site by example, given ask!() currently targets one statically-known audience per call site by design; (d) what is explicitly out of scope for a first version. Write the spike as docs/superpowers/specs/2026-08-18-ranting-if-companion-crate.md, following the shape of existing specs in that directory. Do NOT create any new crate, .rs file, or Cargo.toml -- this is a spike only, prose and examples. Update ROADMAP.md Phase 9 item 5 with a short 'PROPOSED (2026-08-18 spike -- NOT implemented; maintainer decision needed)' paragraph pointing at the new spec. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml as CLAUDE.md requires (this should be a no-op gate pass since no .rs/.toml file changes).
