Implement Phase 6 item 11 "Zero-length article still emits its separator" from ROADMAP.md: this is a real bug producing user-visible wrong output, found by item 10's German lexicon (hole 6, see ranting_i18n/README.md and its tests/holes.rs). When inflect_article_custom returns an empty string to mean "no article here" - which German needs for the indefinite plural, Hunde bellen - the separator between article and noun is emitted anyway, so the placeholder renders with a leading space and a doubled space mid-sentence. Read ranting_i18n/tests/holes.rs's hole_6 test first; it pins the current wrong behavior with a probe. Fix handle_placeholder_impl in src/lib.rs so a zero-length article emits no separator. Note that elide_article_custom cannot repair this - the post-assembly splice is skipped when the recorded article span is empty, so the hook is never called - and that skip_article() is the wrong tool because it is per-entity and unconditional, so it cannot mean "no article in the plural only" and would swallow der/die/das too. Existing English say!() output must be unchanged. Update the hole_6 test in ranting_i18n to assert the corrected output and strike the entry from that README, add integration tests in tests/ranting/, and run the gate inside ranting_i18n as well as at the repo root since the root cargo test does not compile that crate. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 12 "Re-export say_with! and derive_ranting from ranting" from ROADMAP.md: found by item 10 as hole 1 (see ranting_i18n/README.md). ranting re-exports say, ack, nay, heed, Heed, ask, boxed_ranting_trait and ref_ranting_trait but not say_with and not derive_ranting, so a crate depending only on ranting's public API cannot construct a call carrying a NarrationContext - which makes all twelve _with_context hooks dead weight to a companion crate and puts dialect-selected locales out of reach. This looks like an oversight rather than a decision: every other macro is re-exported, and ask! was itself found unexported and fixed the same way in Phase 5. Add the missing re-exports in src/lib.rs, confirm from ranting_i18n that say_with! is now reachable without depending on ranting_derive, and update that crate's README hole 1 entry to record it as closed. Add integration tests in tests/ranting/ and update docs/API.md and CLAUDE.md if they enumerate the re-exported macros. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 13 "The gate must cover sibling crates" from ROADMAP.md: this repo is not a cargo workspace - ranting, ranting_core, ranting_derive and now ranting_i18n each have their own Cargo.lock - so cargo test at the repo root never compiles ranting_i18n. Item 10's crate happened to pass its own gate, but the overnight loop's gate could not have caught a broken lexicon; that was verified after the fact by hand, not by the gate. Make the gate cover every sibling crate: update scripts/overnight_loop.sh so its pre-flight check and its per-task gate run cargo fmt --check, cargo clippy -- -D warnings and cargo test in each crate directory that has its own Cargo.toml, not only at the repo root. Keep the script's existing behavior otherwise, including the incremental task consumption and the per-repo LOG_DIR. Verify by deliberately breaking something in ranting_i18n in your working tree, confirming the gate now fails, then reverting it. Document the gate's scope in CLAUDE.md's Commands section. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 14 "Numeral count channel, plus case on Ranting::inflect" from ROADMAP.md: this is the owed hook-signature break and both halves must land together so it happens once. Read docs/superpowers/specs/2026-08-13-number-categories.md first, especially its Recommendation and its three Open Questions, plus hole 2 in ranting_i18n/README.md. Half one, the count channel: keep as_plural: bool exactly as it is and authoritative for English, and additionally pass the placeholder's numeral, when it has one, to the hooks as a small public value type with an explicit no-numeral-here state; per open question 1 make it a struct carrying visible fraction digits so the existing 1.0-is-not-one behavior at src/lib.rs:379 stays expressible; per open question 2 put it on all six hooks since Arabic and Slavic verb agreement is number-sensitive beyond singular/plural. ranting must never map the count to a plural category - that is the companion crate's job. Half two, case on inflect: German declines the noun itself (dative plural den Hunden, genitive des Hauses) but Ranting::inflect(to_plural, uc) has no case parameter, so the form has to be carried on the entity; add the case parameter at this same signature-break site. Existing say!() output must be unchanged. Add integration tests in tests/ranting/, update ranting_i18n to use the new parameters and strike hole 2 from its README, document both in docs/EXTENSIBILITY.md and docs/API.md, add a CLAUDE.md "Non-obvious behaviors" bullet, and run the gate in ranting_i18n as well as at the root. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 15 "Many exposes its length as the placeholder count" from ROADMAP.md: this is open question 3 of docs/superpowers/specs/2026-08-13-number-categories.md and depends on item 14's count channel existing, so check for it first and stop if it is absent. Many<T> in src/collections.rs is the one entity that genuinely knows its own item count, so a bare {many_items} placeholder with no numeral should supply Many's len() as the count to the hooks rather than reporting no-numeral-here; the spec notes this is the only path to categorial number without a placeholder-grammar change. Leave Maybe and Box reporting no-numeral-here, and leave the as_plural: bool behavior of all three exactly as it is. Add integration tests in tests/ranting/ covering empty, single-item and multi-item Many, and note the behavior in docs/EXTENSIBILITY.md and CLAUDE.md. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 16 "is_first_person_subject as an overridable hook" from ROADMAP.md: this is open question 1 of docs/superpowers/specs/2026-08-13-pronoun-inventory.md, which calls it the one named gap with no fork-side workaround. Read that spec first. Today is_first_person_subject is a hard-coded matches!("I" | "we") in ranting_core/src/grammar.rs, so a non-English fork declaring its own first-person subject label cannot get narration-viewpoint resolution (narration::resolve_viewpoint in src/narration.rs) to recognize it. Add a Ranting trait hook defaulting to exactly today's behavior so English output is unchanged, and route resolve_viewpoint through it. Keep the change small and additive - the spec explicitly scopes it that way. Add integration tests in tests/ranting/ with a fork declaring a non-English first-person label and getting correct viewpoint resolution, document the hook in docs/EXTENSIBILITY.md and docs/API.md, and add a CLAUDE.md "Non-obvious behaviors" bullet. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 17 "Sentence detection beyond Latin punctuation" from ROADMAP.md: PH_START in ranting_core/src/grammar.rs contains (?:^|[.?!]\s+|\{\{) - it decides a placeholder is sentence-initial, and so should auto-capitalize, only after an ASCII . ? or ! followed by whitespace. That silently never fires after a Greek question mark ; (and Greek has grammatical case, so this is a real miss), after Japanese or Chinese full-width punctuation which takes no following space, or after Urdu; and in Spanish the opening inverted question mark sits between the punctuation and the placeholder so the match fails there too. Phase 6 item 6 routed capitalization through a Ranting::capitalize hook but did not touch detection, which is upstream of it. Widen the recognized sentence-terminator and opening-punctuation sets and make the following-whitespace requirement optional where the script does not use one. Also address open question 2 of docs/superpowers/specs/2026-08-13-word-order-feasibility.md while here: expose "is this placeholder sentence-initial" to the capitalize hook as an explicit signal rather than only the implicit uc: bool. Existing English say!() output must be unchanged - verify against the full existing suite, not just new tests. Add integration tests in tests/ranting/ covering Greek, Spanish and Japanese, and document in docs/EXTENSIBILITY.md and CLAUDE.md. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 18 "Dative/genitive on GrammaticalCase" from ROADMAP.md: this is a documentation-only task, write no production code. Produce docs/superpowers/specs/2026-08-13-grammatical-case-inventory.md. The gap is hole 3 in ranting_i18n/README.md: GrammaticalCase carries English's inventory (Name, Subjective, Objective, PossessiveDeterminer, PossessivePronoun, Reflexive, Hidden), German has four cases, and @ means accusative-or-dative so dem/der are unreachable from any marker. The sharper finding to analyse is that once the entity must carry the case to reach dative at all, the case parameter becomes ignorable - say!("{the =0}") and say!("{the @0}") render identically for such a fork, which is pinned by hole_3 in ranting_i18n/tests/holes.rs. Score at least: adding dative/genitive variants to the enum; making the case channel open-ended like NounClass; and changing nothing and documenting that a fork past two cases carries case on the entity. Note for each whether it needs a new placeholder marker, since the marker set is a ✅ Locked architecture decision, and whether it is breaking. The pronoun-inventory spec set the precedent that "change nothing, document it" is a legitimate conclusion. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 19 "Case marking and pronoun display share one hook" from ROADMAP.md: found by item 10 as hole 5 (see ranting_i18n/README.md and hole_5 in its tests/holes.rs). A case marker does two jobs at once - it tells inflect_article_custom the noun's grammatical role and it switches the noun slot from the name to a pronoun - so a fork that overrides inflect_pronoun_custom to return the noun's name, which tests/ranting/grammatical_case.rs demonstrates and describes as what a case-declining fork typically does, thereby loses real pronouns for that entity: say!("Ich sehe {@0}.", hund) renders "Ich sehe Hund." instead of "Ich sehe ihn." Provide a way to say "case-mark this placeholder but render the name", separating the two jobs. Prefer a solution that does not add a new placeholder marker if one exists, since the marker set is a ✅ Locked architecture decision; if a new marker is genuinely required, record the rejected alternatives in the ROADMAP item's sub-bullets. Existing say!() output must be unchanged. Add integration tests in tests/ranting/, update ranting_i18n to drop its Render::Name workaround and strike hole 5 from its README, document in docs/EXTENSIBILITY.md, and run the gate in ranting_i18n as well as at the root. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 20 "Document the word-order boundary" from ROADMAP.md: this is open question 1 of docs/superpowers/specs/2026-08-13-word-order-feasibility.md, which recommends the boundary statement land in docs/EXTENSIBILITY.md in full with one-line pointers from CLAUDE.md and README.md. This is a documentation-only task, write no production code. Read that spec first and reuse its own single-sentence version verbatim as the pointer text: ranting inflects words within a template; the order of those words is the template's, and the template is the caller's - so a non-English application needs one template per language, and no inflection hook will ever change that. Write it as a permanent boundary, not a TODO, matching how docs/superpowers/specs/2026-08-12-input-parsing-feasibility.md insisted pronoun reversal be documented as fundamentally ambiguous rather than not-yet-implemented, and include the spec's named list of constructions that stay out of reach (German verb-second with separable prefixes, SOV with postpositions, VSO, suffixed definite articles, adjective movement, sentence-final particles). Add the item 10 finding that German's attributive adjectives are prenominal while the degree slot is post-noun, so there is no German sentence in which the adjective hook's output is correct - it is the sharpest single illustration of the boundary. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 21 "Document non-English pronoun inventories" from ROADMAP.md: this is the entire follow-up that docs/superpowers/specs/2026-08-13-pronoun-inventory.md authorizes - its recommendation is option (c) in thin form, change nothing and document what already exists. This is a documentation-only task, write no production code. Read that spec first. Add a docs/EXTENSIBILITY.md section on non-English pronoun inventories covering: subjective() -> &str being an open uninterpreted channel; the five PronounCase arms plus inflect_verb_custom being the fork's own table; declaring a #[ranting(subject = "...")] struct rather than reusing Noun; the T-V precedence rule (addressee label beats ctx.register beats nothing); and the unwrap_or(It) degrade as the documented consequence of leaving an arm unhandled. Add the item 10 confirmation that an unrecognized subjective() degrades silently - a declined verb takes the catch-all arm of english::inflect_verb and renders the bare form, "Der Hund walk." - since that is the cost of the closed-enum decision made concrete. Then flip the "SubjectPronoun is a closed English enum" row in ROADMAP.md's Key Architecture Decisions table to ✅ Locked, pointing at this spec. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 22 "Per-language template selection spike" from ROADMAP.md: this is a documentation-only task, write no production code. Produce docs/superpowers/specs/2026-08-13-template-selection.md. docs/superpowers/specs/2026-08-13-word-order-feasibility.md recommends per-language template sets and says the caller selects the template before the say!() call, but never addresses how: say!() parses its literal at compile time, so the selection cannot be a runtime lookup in a translation catalogue and must be a literal match lang { Lang::De => say!("..."), Lang::En => say!("...") } written out at every call site, which scales as languages times sentences and is the real ergonomic cost of that recommendation. Read that spec and ranting_derive/src/lib.rs's say!() codegen first. Score at least: leaving it to the caller and documenting it; a macro that takes a template set and expands the match; and a per-language template-set type declared once with a macro selecting from it by a runtime language value. State for each whether it preserves the ✅ Locked "compile-time parsing + runtime inflection" architecture decision, since anything resolving templates at runtime does not. End with a recommendation and the rejected alternatives with reasons. Concluding "leave it to the caller, documented" is an acceptable outcome. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 23 "Spanish reference lexicon" from ROADMAP.md: the second acceptance test, run it after the items above. Create a new crate directory ranting_es/ (this repo is NOT a cargo workspace - each crate has its own Cargo.lock - so give it its own Cargo.toml depending on ranting = { path = ".." }), following ranting_i18n/ as the model for structure, README and falsification contract. Read ranting_i18n/README.md first, and docs/EXTENSIBILITY.md section 4.3 which already carries a complete Spanish implementation to build from. Implement Spanish for a deliberately small closed vocabulary using only ranting's public API: el/la/los/las and un/una definite and indefinite articles via NounClass, post-nominal adjective agreement (el gato negro, la casa negra, los gatos negros), verb agreement, tu and usted second-person forms, and the numerals 0 to 12. Spanish is the acceptance test German could not be for adjectives: item 10 found German attributive adjectives are prenominal while the degree slot is post-noun, so no German sentence renders the adjective hook correctly, whereas Spanish adjectives are post-nominal and can exercise it end to end. Same falsification contract as item 10: every place Spanish cannot reach something through the public API is a hole, written down in the crate's README with a test pinning it, never worked around; and cross-reference each hole to the ROADMAP item it belongs to. Keep cargo fmt --check, cargo clippy -- -D warnings and cargo test green in the new crate and at the repo root. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
