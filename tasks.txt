Implement ROADMAP.md Phase 8 item 3, agreeing quantifiers and the mass/count distinction, per its DECIDED block (maintainer ruling 2026-08-17, full recommended package) and docs/superpowers/specs/2026-08-15-quantifier-determiners.md. Implement part (b) first: a defaulted `Ranting::is_mass() -> bool` trait method declared via `#[ranting(mass)]` (mirror the existing `gender` attribute -> `noun_class()` mechanism exactly) plus `Noun::with_mass()`; do not encode mass in NounClass, they are orthogonal axes per the spike. Then part (a): add the six quantifier word/pair variants (no, every<->all, each, either/neither, much/many, less/fewer) as new arms on the existing ArticleKind/ArticleOrSo pair (no sibling type -- read .claude/rules/extension-hooks.md's inflect_article_custom paragraph and get_article_or_so's hook-offer protocol first), offering every new word to inflect_article_custom_with_context with the same GrammaticalCase/NounClass/count signals the existing article arms pass today. Do not mark ArticleKind #[non_exhaustive] (the spike recommends against it). For the open sub-decisions the spike left to the implementer -- the word list's cut line, mass-AAnSome rendering (some vs. elision), and error-vs-override behavior for a marker contradiction like `{each +item}` -- resolve each consistently with the spike's own stated rationale and record the resolution inline in ROADMAP.md item 3's landed note, don't leave it implicit in code alone. Verify and fix the two rendering hazards the spike recorded against the built crate: `{no $n item}` at n=1 currently renders "Noes 1 item" (open-pass word falling through to the pre-noun verb path), and `{Some info}` on a singular currently renders "An information" (adapt_article discarding `some` for a/an) -- both must render correctly once mass/quantifier support lands. Add integration tests to tests/ranting/ (register in main.rs) covering all six quantifier words at singular and plural, the mass-noun cases, and the `{are no ?$n item}` idiom from the spike. Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml (., ranting_core, ranting_derive, ranting_i18n, ranting_es, ranting_ar, ranting_ja, ranting_gaps, ranting_es_gaps) as CLAUDE.md requires.
Implement ROADMAP.md Phase 8 item 4, ordinals, per its DECIDED block (maintainer ruling 2026-08-17) and docs/superpowers/specs/2026-08-15-ordinal-numerals.md. This is an accepted major-version break: add `NumeralKind::Ordinal` and a new public `NumeralStyle::Ordinal` variant carrying the same real `count: Option<i64>` the cardinal channel already carries, plus the doubled marker `##var` (word-spelled, e.g. `{the ##n attempt}` -> "the third attempt") AND `$$var` (digit-spelled, e.g. "3rd") in the same break, per the ruling. Retype the currently-stringly-typed `plurality` dispatch (currently `&str`) alongside this work, fixing the two silent-failure sites the spike identified (`{##n attempt}` must not silently render as a cardinal and agree plural). Give `nr`'s alternation a one-repetition restriction at the same time it is widened for the new markers, mirroring the open-pass `pre` one-repetition precedent documented in .claude/rules/placeholder-grammar.md. Agreement decouples from the ordinal itself: `as_pl` falls through to `noun.is_plural()` ("the third attempt", not "attempts") while `count` still flows through to `inflect_numeral_custom`, which is what Spanish/Arabic ordinal gender agreement needs -- verify this doesn't regress ranting_es/ranting_ar. Because this breaks the public NumeralStyle enum's exhaustive matches, fix all four falsifier crates' (ranting_i18n, ranting_es, ranting_ar, ranting_ja) match arms for the new variant, and bump the version of ranting, ranting_core and ranting_derive (version-locked together per CLAUDE.md) to the next major version in their Cargo.toml files -- do NOT run cargo publish or push anything, that stays a deliberate human step. Add integration tests to tests/ranting/ (register in main.rs) for both ##var and $$var at several counts including 1, 2, 3, 11-13 (English ordinal irregularities). Before reporting done, run cargo fmt --check, cargo clippy --all-targets -- -D warnings and cargo test in every directory that has a Cargo.toml (., ranting_core, ranting_derive, ranting_i18n, ranting_es, ranting_ar, ranting_ja, ranting_gaps, ranting_es_gaps) as CLAUDE.md requires -- this task is expected to require fixing compile errors in all four falsifier crates due to the exhaustive-match break, budget time for that.
