Implement Phase 6 item 11 "Numeral count channel on the inflection hooks" from ROADMAP.md: docs/superpowers/specs/2026-08-13-number-categories.md recommends adopting option (b) narrowed to a count channel, and says to land it inside Phase 6 item 5's signature change - but item 5 already shipped (commit 67c5d68c), so this is now a standalone follow-up. Read that spec first, especially its Recommendation section and its three Open Questions. Keep as_plural: bool exactly as it is and authoritative for English; additionally pass the placeholder's numeral, when it has one, to the hooks as a small public value type with an explicit no-numeral-here state. Per the spec's open question 1 make that type a struct carrying visible fraction digits rather than a bare i64 or Option<i64>, so the existing 1.0-is-not-one behavior at src/lib.rs:379 is expressible to a fork. Per open question 2 put the count on all six hooks, not just article/pronoun/adjective, since Arabic and Slavic verb agreement is number-sensitive beyond singular/plural. ranting must never map the count to a plural category - categorization is the companion crate's job. Existing say!() output must be unchanged. Add integration tests in tests/ranting/, document the new parameter in docs/EXTENSIBILITY.md and docs/API.md, and add a CLAUDE.md "Non-obvious behaviors" bullet. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 12 "Many exposes its length as the placeholder count" from ROADMAP.md: this is open question 3 of docs/superpowers/specs/2026-08-13-number-categories.md and depends on item 11 having landed, so check that the count channel exists before starting and stop if it does not. Many<T> in src/collections.rs is the one entity that genuinely knows its own item count, so a bare {many_items} placeholder with no numeral should supply Many's len() as the count to the hooks rather than reporting no-numeral-here. The spec notes this is the only path to categorial number without a placeholder-grammar change. Leave Maybe and Box reporting no-numeral-here, and leave the as_plural: bool behavior of all three exactly as it is. Add integration tests in tests/ranting/ covering empty, single-item and multi-item Many, and note the behavior in docs/EXTENSIBILITY.md and CLAUDE.md. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 13 "is_first_person_subject as an overridable hook" from ROADMAP.md: this is open question 1 of docs/superpowers/specs/2026-08-13-pronoun-inventory.md, which calls it the one named gap with no fork-side workaround. Read that spec first. Today is_first_person_subject is a hard-coded matches!("I" | "we") in ranting_core/src/grammar.rs, which means a non-English fork declaring its own first-person subject label cannot get narration-viewpoint resolution (narration::resolve_viewpoint in src/narration.rs) to recognize it. Add a Ranting trait hook defaulting to exactly today's behavior so English output is unchanged, and route resolve_viewpoint through it. Keep the change small and additive - the spec explicitly scopes it as such. Add integration tests in tests/ranting/ with a fork declaring a non-English first-person label and getting correct viewpoint resolution, document the hook in docs/EXTENSIBILITY.md and docs/API.md, and add a CLAUDE.md "Non-obvious behaviors" bullet. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 14 "Sentence detection beyond Latin punctuation" from ROADMAP.md: PH_START in ranting_core/src/grammar.rs is (?P<pre>(?:^|[.?!]\s+|\{\{)?+) - it decides a placeholder is sentence-initial, and so should auto-capitalize, only after an ASCII . ? or ! followed by whitespace. That silently never fires after a Greek question mark ; (and Greek has grammatical case, so this is a real miss), after Japanese or Chinese 。！？ (which take no following space), or after Urdu ۔; and in Spanish the opening ¿ sits between the punctuation and the placeholder so the match fails there too. Phase 6 item 6 routed capitalization through a Ranting::capitalize hook but did not touch detection, which is upstream of it. Widen the recognized sentence-terminator and opening-punctuation sets, and make the following-whitespace requirement optional where the script does not use one. Also address open question 2 of docs/superpowers/specs/2026-08-13-word-order-feasibility.md while here: expose "is this placeholder sentence-initial" to the capitalize hook as an explicit signal rather than only the implicit uc: bool. Existing English say!() output must be unchanged - verify against the full existing suite, not just new tests. Add integration tests in tests/ranting/ covering Greek, Spanish ¿ and Japanese 。, document in docs/EXTENSIBILITY.md and CLAUDE.md. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 15 "Document the word-order boundary" from ROADMAP.md: this is open question 1 of docs/superpowers/specs/2026-08-13-word-order-feasibility.md, which recommends the boundary statement land in docs/EXTENSIBILITY.md in full with one-line pointers from CLAUDE.md and README.md. This is a documentation-only task, write no production code. Read that spec first and reuse its own single-sentence version verbatim as the pointer text: ranting inflects words within a template; the order of those words is the template's, and the template is the caller's - so a non-English application needs one template per language, and no inflection hook will ever change that. In docs/EXTENSIBILITY.md write it as a permanent boundary, not a TODO, matching how docs/superpowers/specs/2026-08-12-input-parsing-feasibility.md insisted pronoun reversal be documented as fundamentally ambiguous rather than not-yet-implemented, and include the spec's named list of constructions that stay out of reach (German verb-second with separable prefixes, SOV with postpositions, VSO, suffixed definite articles, adjective movement, sentence-final particles). Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 16 "Document non-English pronoun inventories" from ROADMAP.md: this is the entire follow-up that docs/superpowers/specs/2026-08-13-pronoun-inventory.md authorizes - its recommendation is option (c) in thin form, change nothing and document what already exists. This is a documentation-only task, write no production code. Read that spec first. Add a docs/EXTENSIBILITY.md section on non-English pronoun inventories covering: subjective() -> &str being an open uninterpreted channel; the five PronounCase arms plus inflect_verb_custom being the fork's own table; declaring a #[ranting(subject = "...")] struct rather than reusing Noun; the T-V precedence rule (addressee label beats ctx.register beats nothing); and the unwrap_or(It) degrade as the documented consequence of leaving an arm unhandled. Then flip the "SubjectPronoun is a closed English enum" row in ROADMAP.md's Key Architecture Decisions table from 🎯 v1.3 to ✅ Locked, pointing at this spec. Per the spec's third open question, prefer pointing at the compiling tests (tests/ranting/grammatical_case.rs and the noun_class tests) over a prose-only impl where the two would otherwise duplicate, since tests cannot drift. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
Implement Phase 6 item 17 "Per-language template selection spike" from ROADMAP.md: this is a documentation-only task, write no production code. Produce docs/superpowers/specs/2026-08-13-template-selection.md. docs/superpowers/specs/2026-08-13-word-order-feasibility.md recommends per-language template sets and says the caller selects the template before the say!() call, but it never addresses how: say!() parses its literal at compile time, so the selection cannot be a runtime lookup in a translation catalogue and must be a literal match lang { Lang::De => say!("..."), Lang::En => say!("...") } written out at every call site, which scales as languages times sentences and is the real ergonomic cost of the recommendation. Read that spec and ranting_derive/src/lib.rs's say!() codegen first. Score at least: leaving it to the caller and documenting it; a macro that takes a template set and expands the match for it; and a per-language template-set type declared once with a macro that selects from it by a runtime language value. State for each whether it preserves the ✅ Locked "compile-time parsing + runtime inflection" architecture decision, since anything resolving templates at runtime does not. End with a recommendation and the rejected alternatives with reasons. Concluding "leave it to the caller, documented" is an acceptable outcome. Mark the item done in ROADMAP.md by inserting "✅ " directly before the item's bold title and record the implementation notes as sub-bullets under it.
